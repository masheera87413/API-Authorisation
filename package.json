{
  "name": "api-auth-mongo",
  "version": "1.0.0",
  "description": "API authorization using headers with MongoDB (no JWT)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^7.0.0"
  }
}